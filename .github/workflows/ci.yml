name: CI

on:
  push:
    branches:
      - 'multi-packages'
      - 'beta'
      - 'alpha'
  pull_request:
    branches:
      - '**'

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Toss Payments 공통 설정
        uses: ./.github/actions/configure
        with:
          npm_token: ${{ secrets.NPM_TOKEN }}
      - name: Node.js 환경 설정
        uses: actions/setup-node@v1
        with:
          node-version: '12.16.3'
      - name: 'SINCE_FROM_REF 환경변수 설정'
        run: |
          if [[ ${{ github.ref }} == 'refs/heads/multi-packages' ]]; then
            echo "SINCE_FROM_REF=$(git rev-parse HEAD~1)" >> $GITHUB_ENV
          elif [[ ${{ github.ref }} == 'refs/heads/staging'* ]]; then
            echo "SINCE_FROM_REF=$(git rev-parse HEAD~1)" >> $GITHUB_ENV
          elif [[ ${{ github.event_name }} == 'pull_request' ]]; then
            echo "SINCE_FROM_REF=$(git rev-parse origin/${{ github.base_ref }})" >> $GITHUB_ENV
          else
            echo "SINCE_FROM_REF=$(git rev-parse origin/multi-packages)" >> $GITHUB_ENV
          fi
      - name: yarn install
        run: yarn install --immutable
      - name: Run Test
        run: yarn workspaces since run test $SINCE_FROM_REF --jobs=5
      - name: 릴리즈
        if: github.ref == 'refs/heads/multi-packages' || github.ref == 'refs/heads/beta' || github.ref == 'refs/heads/alpha'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          yarn lerna version --conventional-commits --conventional-prerelease --exclude-dependents --yes --git-remote origin
          yarn lerna publish from-git --yes --concurrency=2
